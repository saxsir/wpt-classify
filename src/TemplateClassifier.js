/**
 * テンプレートを判定（分類）処理をまとめるクラス
 */
(function() {
  'use strict';

  var Section = require('./Section');

  function TemplateClassifier() {
  }

  TemplateClassifier.prototype.matchingTemplate = function(blocks) {
    //TODO: utils.jsの判定処理をコピーしてくる
    return -1;
  };

  module.exports = TemplateClassifier;
}());
